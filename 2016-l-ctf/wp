pwn100

这题没给libc，而且只有puts函数，不好用dynelf。所以只能用puts先泄露read函数的地址，然后推算出syscall的地址，最后进行0x3b号系统调用即可。

pwn200

简单得栈溢出，用checksec查一下，nx关了。而且strcpy函数正好可以任意地址写。我们把free@got覆盖位shellcode地址即可。

pwn300

这题需要自己溢出得到libgetshell，然后分析一下，有个可以直接得到shell的getshell函数，用dynelf泄露得到libgetshell的加载地址，然后根据dump下来的libgetshell得到getshell函数的偏移，相加即可得到getshell函数的地址，然后溢出一次就可以得到shell了。
